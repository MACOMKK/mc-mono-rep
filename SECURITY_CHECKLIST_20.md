# Checklist de segurança (20 itens) — status atual do monorepo

> Auditoria feita em 2026-08-26 com base no código real (Edge Functions, migrations, `.gitignore`, `config.toml`, `vercel.json`). Complementa o `SECURITY_PENDENTES.md`.

| # | Item | Status | Evidência |
|---|---|---|---|
| 1 | Esconder API Keys | ✅ JÁ TEM | `.gitignore` ignora `.env*`, só `.env.example` versionado |
| 2 | Limpar secrets do git | ⚠️ PARCIAL | sem evidência de purge de histórico do git |
| 3 | Public Key DB | ✅ JÁ TEM | client usa `VITE_SUPABASE_ANON_KEY`; `service_role` só em Edge Functions |
| 4 | Ativar RLS | ✅ JÁ TEM | RLS habilitada em 44 migrations; ajuste final aplicado em 2026-08-26 |
| 5 | Criptografia de dados | ⚠️ PARCIAL | `google_calendar_refresh_token` migrado para Supabase Vault (2026-08-27, migration `20260827100000_move_google_calendar_refresh_token_to_vault.sql`) — leitura/escrita/reconexão/desconexão implementadas em `intranet-api`; falta só confirmar criação de evento com Meet em produção (bloqueado por Google Calendar API desabilitada no Cloud Console, item à parte, não é bug da migração). `push_subscriptions` (passo 2) e `colaboradores.data_nascimento` (passo 3) avaliados e descartados conscientemente — baixo ganho de segurança vs. custo/risco. `colaboradores.cpf` (passo 4) desenhado (pgcrypto + hash HMAC) e adiado por decisão do usuário. Decisões detalhadas no histórico do plano de segurança |
| 6 | Auth server side | ✅ JÁ TEM | Edge Functions exigem JWT (ex: `enviar-termo-gmail`) |
| 7 | Restringir acessos | ⚠️ PARCIAL | checks de nível/admin existem (`central-api`, `plataforma-api`) mas não uniformes |
| 8 | Bloquear Mass Assignment | ⚠️ PARCIAL | alguns endpoints filtram campos (`catalog-api` remove `id`), sem padrão geral |
| 9 | Proteger cookies | ❌ PRECISA | sessão do Supabase Auth fica em `localStorage` (client puro via `createClient` em `packages/api-client/src/supabaseClient.js`, sem `@supabase/ssr`/servidor — não há onde setar cookie `httpOnly`), não em cookie protegido. Risco real não é CSRF, é XSS lendo o token via JS. Verificação de XSS feita em 2026-08-27: sem `dangerouslySetInnerHTML`/`innerHTML`/`eval`/`document.write` com dado de usuário em nenhum app (único `dangerouslySetInnerHTML` é CSS estático de tema em `crm/chart.jsx`); `react-quill` e `react-markdown` são dependências declaradas mas não importadas em nenhum lugar do código — sem superfície de rich text/markdown ativa. Único vetor real achado: `href` dinâmico recebendo URL de usuário (aceitava `javascript:...`) em `QuickLinks.jsx` (2x), `AnnouncementDetailsDialog.jsx`, `QuickAccessGrid.jsx` e `Documents.jsx` (2x) na intranet — corrigido em 2026-08-27 via `sanitizeUrl()` (`apps/intranet/src/lib/utils.js`), que só permite `http:`/`https:`/`mailto:`/`tel:`. Migrar a sessão pra cookie `httpOnly` exigiria reescrever a arquitetura de auth dos 7 SPAs (hoje sem camada de servidor) — custo alto pra mitigar um risco sem exploração ativa mapeada hoje. Adiado por decisão do usuário (2026-08-27); rebaixado na ordem de prioridades |
| 10 | Hash nas senhas | ✅ JÁ TEM | 100% delegado ao Supabase Auth |
| 11 | Rate limit | ⚠️ PARCIAL | só no login (nativo do Supabase Auth); demais Edge Functions sem limite. Mapeado em 2026-08-27: `servicos-api` `action: 'create'` (linha ~1502) cria `solicitacao_pagamento` validando `fornecedor_id`/`categoria_id`/`aprovador_destino_id` reais (não é payload livre), mas sem nenhum limite de frequência por colaborador — cada create bem-sucedido dispara `notifyAprovadorNovaSolicitacao` → `insertNotificacao` → `sendPushToColaborador` (linha 605), ou seja, um colaborador autenticado com acesso ao módulo pode inundar de push um aprovador específico repetindo `create` em loop. Mesmo padrão sem limite em `notifySolicitantePendencia` (abrir/fechar pendência) e nas ações de aprovar/reprovar/pagar. Exige auth válida (não é anônimo), mas nenhuma Edge Function de negócio tem rate limit por usuário — só o endpoint de login tem, via Supabase Auth nativo |
| 12 | Bot protection | ✅ JÁ TEM | Cloudflare Turnstile no login dos 7 apps |
| 13 | Queries parametrizadas | ✅ JÁ TEM | SQL injection via `orderBy` corrigido no `intranet-api` |
| 14 | Validação dos inputs | ⚠️ PARCIAL | Zod adotado (2026-08-27) via `supabase/functions/_shared/validation.ts` — não em `@macom/validation` como planejado inicialmente: Edge Functions rodam em Deno e só resolvem imports locais dentro da árvore `supabase/functions`, sem precedente de import cruzando pra `packages/*` (workspace npm/Vite, resolvedor diferente); `_shared` já é o lugar usado hoje para código compartilhado entre functions (`cors.ts`). Feito até agora, function por function, testado manualmente sem regressão: `security-check-login-lock`/`security-log-failed-login`/`security-log-login-success` (schema formaliza a forma do body, mantendo o comportamento fail-open original — nunca rejeita, só formaliza o tipo esperado); `admin-create-user` (schema rejeita com 400 tipos claramente errados no body, campos opcionais aceitam `null`/`undefined` pra não quebrar selects do front, mensagens de erro de negócio existentes mantidas); `plataforma-api`/`central-api`/`relatorios-api` (schema `coreDispatcherBodySchema` compartilhado no dispatcher `action`+`entity` — achado: `plataforma-api` é a implementação canônica de acesso/permissões/senha/email, `central-api` delega pra lá fora do contexto `reports-admin`; já existia allow-list por entidade `ENTITY_CONFIG[entity].allowedFields` filtrando os fallbacks genéricos de create/update — não era passthrough total como se supunha; o Zod substitui em `central-api`/`relatorios-api` casts `as` do TypeScript que não validavam nada em runtime). Falta a parte 2 do passo 3 (registry de schema por entidade pros fallbacks genéricos de `central-api`/`relatorios-api`, ~15-20 entidades, incluindo o caso especial de `colaboradores update` com 3 variantes por contexto) e as demais functions: `catalog-api`, `servicos-api`, `crm-api`, `comunicacao-api`, `intranet-api`, `whatsapp-api`, `push-api`, `enviar-termo-gmail`, `processa-fila-email` — ordem detalhada no plano de segurança |
| 15 | Vazar conteúdo | ✅ JÁ TEM | erro cru do Postgres corrigido no `intranet-api` |
| 16 | Restringir uploads | ⚠️ PARCIAL | `storage.from` usado em várias APIs, sem validação de tipo/tamanho |
| 17 | Trim respostas de API | ⚠️ PARCIAL | ajustes pontuais feitos (`logs_auditoria`), sem revisão geral de `SELECT` |
| 18 | Security headers | ⚠️ PARCIAL | `vercel.json` tem headers básicos, falta CSP e HSTS |
| 19 | Forçar HTTPS | ✅ JÁ TEM | garantido pela hospedagem Vercel |
| 20 | Scan de dependências | ✅ JÁ TEM | script `npm run audit` configurado (2026-08-27); `npm audit fix` aplicado (23→10). `@capacitor/cli`: já na versão estável mais recente, vuln é em `xcode`/`uuid` (tooling iOS não usado) — risco aceito. `sharp` (vite-plugin-pwa/assets-generator): corrigido via `overrides` no `package.json` raiz forçando `sharp@^0.35.4` (10→7 vulnerabilidades); testado build:admin, build:crm e `generate:pwa-icons` sem regressão. `react-router-dom` atualizado de 6.26.0 para 7.18.2 no root e nos apps que fixavam versão própria (crm, servicos, relatorios, intranet — hoisting unificou tudo pra v7); testado `test:run` (mesmas 9 falhas pré-existentes, sem regressão), `typecheck` (mesmos 1195 erros pré-existentes), build de todos os 6 apps e navegação manual em central/admin/crm/relatorios/intranet/servicos (7→5 vulnerabilidades). Restam 5 (react-quill sem fix real, uuid/xcode em tooling iOS não usado) — ambos risco aceito, sem correção disponível que não seja regressiva. Nota (2026-09-21): `@capacitor/cli` e o restante do shell nativo Android foram removidos do `servicos` (app não vai mais ser publicado em loja) — a vuln em `uuid`/`xcode` mencionada acima deixou de existir como dependência |

## Prioridades reais (itens que realmente faltam)

Ordem do menos arriscado (implementar primeiro) para o mais arriscado:

1. **#14 — Validação de inputs**: em andamento desde 2026-08-27 (ver evidência na tabela acima) — Zod adotado via `supabase/functions/_shared/validation.ts`, 4 de ~16 functions feitas (`security-check-login-lock`, `security-log-failed-login`, `security-log-login-success`, `admin-create-user`), testadas manualmente sem regressão. Maior risco: mexe em todas as funções e pode passar a rejeitar payloads que hoje funcionam — seguindo function por função, testando o fluxo real antes de avançar. Próximas: `central-api`, `relatorios-api`, `plataforma-api` (schema compartilhado).
2. **#9 — Proteger cookies**: rebaixado em 2026-08-27 — migrar a sessão do Supabase Auth pra cookie `httpOnly` exige reescrever a arquitetura de auth dos 7 SPAs (não existe camada de servidor hoje), e a verificação de XSS feita nessa data não encontrou vetor ativo relevante além do `href` já corrigido (ver evidência na tabela acima). Fica como melhoria de defesa em profundidade, não bloqueio — retomar só se surgir XSS real ou disposição de reestruturar auth.

`#20 — Scan de dependências` saiu da lista: concluído em 2026-08-27 (script `npm run audit` + 3 etapas de correção testadas isoladamente, ver evidência na tabela acima). Ainda não configurado Dependabot/CI automático — não bloqueia o item, mas fica como melhoria futura.

`#5 — Criptografia de dados` saiu da lista de prioridades: virou `PARCIAL` em 2026-08-27 (refresh_token do Google Calendar migrado pro Vault; demais colunas avaliadas e descartadas/adiadas conscientemente — ver evidência na tabela acima). Segue o mesmo padrão dos outros `PARCIAL` (2, 7, 8, 11, 16, 17, 18): melhoria incremental sobre base que já existe, não bloqueia nada.

Os itens marcados como **PARCIAL** (2, 7, 8, 11, 16, 17, 18) são melhorias incrementais sobre uma base que já existe, não bugs abertos.
